package cardgamelibary;

import java.util.List;

/**
 * A card that can actually be played. Probably all cards subclass this but they don't technically have to.
 * @author 42jpa
 *
 */
public interface PlayableCard extends Card{

	
//this can exist if we want it to. I'm not sure if it needs to.
	
	
}
